%0 <= load %1, %2;
%3 <= load %1, %2;
=
%0 <= load %1, %2;
%3 <= mov %0;

%0 <= store %1, %2;
%3 <= load %0, %2;
=
%0 <= store %1, %2;
%3 <= mov %0;

%0 <= store %1, %2;
%0 <= store %3, %2;
=
%0 <= store %3, %2;

%0 <= load %1, %2;
%1 <= store %0, %2;
=
%0 <= load %1, %2;
